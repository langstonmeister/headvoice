# Note: llama-cpp-python must be installed via setup_env.py for Metal support on macOS
# CMAKE_ARGS="-DGGML_METAL=ON" pip install llama-cpp-python
sounddevice          # Audio recording and wake word stream
soundfile            # Audio file I/O
numpy                # Audio buffer handling
openwakeword         # Wake word detection (open source, no API key needed)
python-dotenv        # .env file loading
dimits               # Text-to-speech synthesis
requests             # For web fallback (future)
beautifulsoup4       # Web scraping (future)
lxml                 # HTML parsing (future)
openai               # OpenAI API fallback (future)
platformdirs         # Cross-OS path management
